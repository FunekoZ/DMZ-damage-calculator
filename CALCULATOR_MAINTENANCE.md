# 计算器推荐与命中排列维护说明

本文档记录推荐、概率注释与命中排列计算规则，避免后续修复一个分支时让另一个问题回归。

## 项目结构与脚本边界

- `index.html`：只维护页面 DOM 结构和静态资源加载顺序。
- `assets/css/app.css`：维护全部外观、布局和响应式样式。
- `assets/js/weapons-data.js`：由 `sync_weapons.py` 生成的内置枪械回退数据，不应手工维护。
- `assets/js/app.js`：维护计算、状态、渲染、交互以及可选的 `weapons.json` 刷新逻辑。
- `weapons.json`：可编辑的枪械配置源文件；修改后运行 `python sync_weapons.py`。
- `sync_weapons.py`：校验 `weapons.json` 并重新生成回退数据声明。

`weapons-data.js` 与 `app.js` 使用经典脚本共享全局作用域，加载顺序不得颠倒。必须继续支持直接双击打开 `index.html`；在 `file://` 下无法读取 `weapons.json` 时，应用使用 `BUILT_IN_WEAPONS` 离线运行。

## 核心数据

每个词条的 `expected()` 结果包含：

- `minShots`：精确枚举或有效模拟中出现的严格最低枪数。
- `shotCounts`：每种枪数的出现次数。
- `shotPatterns`：每种枪数对应的一种示例命中排列。
- `average`：平均枪数，只保留用于诊断，不作为主要推荐依据。
- `exact`：是否由完整排列枚举得到；排列数量不超过 `100,000` 时为 `true`。

命中排列规则：

- 随机命中数量按开火次数计算；多弹丸枪械同一次开火的所有弹丸默认命中同一部位。
- 排列数量不超过 `100,000` 时，枚举所有随机命中位置与部位排列，得到精确分布。
- 排列数量超过 `100,000` 时，才使用固定种子的拒绝采样作为降级方案。
- 任何情况下，随机命中必须全部发生在实际击杀前；排列或样本落在击杀后的命中会被丢弃。

## 推荐计划的唯一入口

所有视图必须调用 `recommendationPlan(results, eligibleIndexes)`，不得在渲染层另写一套最优筛选。

当前规则：

1. 找到严格最低枪数 `strictMin`。
2. 若至少一个词条达到 `strictMin` 的概率不低于 50%，目标枪数为 `strictMin`；否则目标枪数为 `strictMin + 1`，必要时继续上调到存在候选。
3. 候选必须在目标枪数以内击杀的累计概率不低于 50%。
4. 候选中累计概率最高者为 `recommended`。
5. 其他达到门槛但概率更低者为 `unstable`，只能出现在“不主要推荐”注释和“展示全部”中。
6. `recommended` 中能偶尔少于目标枪数的词条为 `rare`，保留在推荐列表并用淡红色提示。

重要不变量：

- `recommended` 与 `unstable` 必须互斥。
- 最优词条区域只能使用去重后的 `recommended + rare`。
- 注释分组必须使用 `Set` 去重，避免同一词条出现两次。

## 无词条

- 单伤模式和单伤对比包含无词条。
- 双伤模式和双伤对比不包含无词条。
- 无词条、破甲和肉伤可以进入主要候选，并使用同一推荐规则。
- 当无词条是最优解，而其他词条只属于淡红或淡蓝概率提示时，仍正常列出无词条及这些提示词条。
- 当其他词条与无词条同等级并列最优时，最优词条区域直接显示“无需词条，无负面即可”。
- 无词条不得单独占一行，应与其他同等级单词条一起填充网格。

## 当前命中不生效的部位倍率词条

- 此类部位倍率词条仍需计算并保留在“展示全部”中。
- 它们不进入 `eligibleIndexes`，因此不参与主要推荐。
- 破甲和肉伤不受本节限制，仍可进入主要候选。
- 最优视图不为被排除的部位倍率词条生成“不生效”注释。

## 概率注释

最优视图：

- `unstable`：淡蓝词条名称，文案“X% 概率多 N枪，故不主要推荐，详情见全部展开”。
- `rare`：淡红词条名称，文案“X% 概率少 N枪，强烈推荐，详情见全部展开”。
- 仅词条名称着色，概率和说明保持普通颜色。
- 相同概率与差值的词条合并；每行最多 3 个词条。
- 刚好 3 个词条时，第一行词条末尾保留冒号，概率说明下一行显示。

展示全部：

- 对所有 `eligibleIndexes` 中存在多个枪数结果的词条显示详情。
- 注释以该词条实际出现概率最大的枪数为基准；若概率相同，采用枪数更少者。
- 单元格主数值也统一显示上述概率最大枪数，不再显示严格最低枪数。
- 相对于该基准，列出其他实际出现枪数的“多 N 枪”或“少 N 枪”概率。

## 跨护甲推荐频率

- 每次计算后，统计当前最优词条表中各词条进入 `recommended` 的护甲行数。
- 单伤、双伤以及对比模式的单伤列和双伤列分别统计，不跨类型混合。
- 忽略“无词条”；出现频率最高的非空词条使用金色标记，并列最高时全部标记。
- 金色说明统一放在结果表标题下方：“金色：最广泛场景下的最优解，最推荐”，不在每个单元格重复。

## 射程曲线

- “曲线”入口与单伤、双伤、对比并列，展示当前枪械和品质下的射程变化。
- 横轴为实际射程，纵轴跟随当前指标显示击杀枪数或 TTK；每条曲线代表一种护甲。
- 用户可独立选择无词条、单伤或当前品质支持的双伤组合，切换后立即重绘。
- 每个射程段使用与普通计算相同的 `expected()` 和 `recommendationPlan()` 得到目标枪数。
- 曲线按射程分段绘制为阶梯线。线段重叠时，仅按护甲默认顺序增加极小视觉偏移并使用不同虚线样式；悬浮提示必须显示未偏移的实际值。
- 曲线必须提供图例、纵向十字线和同一射程下所有护甲的悬浮结果。

## 射程曲线

- “曲线”模式使用当前枪械、品质、命中设置和用户选择的词条，按每个真实伤害射程段重新计算全部护甲。
- 横轴为射程，纵轴随当前指标显示击杀枪数或 TTK；每条曲线代表一种护甲配置。
- 曲线品质色以总表护甲标记色的语义为基础，但针对图表重叠场景拉开色相：紫甲使用紫色、橙甲使用深橙色、红甲使用洋红色、究甲使用金黄色；同品质不同护甲再通过逐级降低饱和度区分，并继续保留线型作为辅助编码。
- 横轴只标出确实造成任一护甲结果变化的射程拐点以及起止点；纵轴只标出曲线实际出现的枪数或 TTK 值，不生成固定等距刻度。枪数模式纵轴只显示整数枪数，不显示由视觉偏移产生的小数。
- 曲线主体保持阶梯语义。重叠的水平段加入适度纵向偏移，重叠的竖直转折使用按默认护甲顺序递增的正向横向偏移和更明显但仍很小的正向斜率，不生成负角度，偏移仅用于辨识。
- 悬浮提示和可展开数据表始终显示未经视觉偏移的真实结果。
- 曲线按钮右侧提供与排行一致的问号说明入口，集中解释计算条件、阶梯线、视觉偏移、刻度、颜色及真实数据查看方式。
- 词条选择变化后立即重新绘制；切换枪械、品质、命中设置或指标后使用最新条件重绘。

## 对比表布局

- 电脑端对比表优先使用可用宽度完整显示，不为每列设置统一的 108px 最小宽度。
- 护甲、差距和数值列保持紧凑，更多宽度分配给单双伤词条列。
- 手机端保留横向滚动，避免词条文字被压缩到无法阅读。

## 历史回归清单

修改推荐或注释逻辑后至少验证：

1. 同一词条同时属于 `recommended` 和 `rare` 时只显示一次。
2. `unstable` 不出现在最优词条列表。
3. 相同概率注释中的词条名称不重复。
4. 高概率达到更低枪数时，目标枪数应直接降低，不应显示“高概率少一枪”。
5. 展示全部中，真正有枪数波动的有效词条必须有注释。
6. 单伤含无词条，双伤不含无词条。
7. 无头部命中时，头部专属正向词条不参与主要推荐，但仍出现在展示全部。
8. 无词条是最优解、其他词条只有淡红或淡蓝提示时，应正常列出这些词条及概率提示。
9. 无词条与其他词条同等级并列最优时，应显示“无需词条，无负面即可”。
10. 展示全部的概率注释和单元格主数值必须以该词条概率最大的枪数为基准；概率相同时选择更少枪数。
11. 六个随机命中部位各一枪、共 15 个槽位时，排列数应为 `C(15,6) × 6! = 3,603,600`，必须降级采样。
12. 最优词条表中出现频率最高的非空词条应标为金色；并列最高时全部标记，对比模式单双伤分别统计。
13. 电脑端对比表在正常结果区域宽度下应优先完整显示六列，手机端才使用最小宽度和横向滚动。
14. 曲线词条变化后应立即重绘；枪数与 TTK 切换后纵轴、提示和数据表必须同步。
15. 曲线视觉偏移不得改变悬浮提示或数据表中的实际枪数和 TTK。
13. 对比表电脑端应使用紧凑列宽，优先在结果区域内完整展示六列。
14. 曲线模式切换词条、枪数/TTK、枪械品质或命中条件后必须重绘，且悬浮值不包含视觉偏移。
15. 紫品质曲线仅提供无词条，橙品质不提供双伤，红/究品质提供单伤和双伤。
