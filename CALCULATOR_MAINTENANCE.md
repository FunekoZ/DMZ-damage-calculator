# 计算器推荐与随机模拟维护说明

本文档记录本轮曾出现的推荐、概率注释与表格展示问题，避免后续修复一个分支时让另一个问题回归。

## 核心数据

每个词条的 `expected()` 结果包含：

- `minShots`：4,000 次有效模拟中出现的严格最低枪数。
- `shotCounts`：每种枪数的出现次数。
- `shotPatterns`：每种枪数对应的一种示例命中排列。
- `average`：平均枪数，只保留用于诊断，不作为主要推荐依据。

随机命中必须全部发生在实际击杀前；落在击杀后的排列会被丢弃并重新抽样。随机命中数量按开火次数计算；多弹丸枪械同一次开火的所有弹丸默认命中同一部位。

## 推荐计划的唯一入口

所有视图必须调用 `recommendationPlan(results, eligibleIndexes)`，不得在渲染层另写一套最优筛选。

当前规则：

1. 找到严格最低枪数 `strictMin`。
2. 若至少一个词条达到 `strictMin` 的概率不低于 50%，目标枪数为 `strictMin`；否则目标枪数为 `strictMin + 1`，必要时继续上调到存在候选。
3. 候选必须在目标枪数以内击杀的累计概率不低于 50%。
4. 候选中累计概率最高者为 `recommended`。
5. 其他达到门槛但概率更低者为 `unstable`，只能出现在“不主要推荐”注释和“展示全部”中。
6. `recommended` 中能偶尔少于目标枪数的词条为 `rare`，保留在推荐列表并用淡红色提示。

重要不变量：

- `recommended` 与 `unstable` 必须互斥。
- 最优词条区域只能使用去重后的 `recommended + rare`。
- 注释分组必须使用 `Set` 去重，避免同一词条出现两次。

## 无词条

- 单伤模式和单伤对比包含无词条。
- 双伤模式和双伤对比不包含无词条。
- 无词条与普通词条使用同一推荐规则，不使用独立替换分支。
- 无词条不得单独占一行，应与其他单词条一起填充网格。

## 当前命中不生效的词条

- 此类词条仍需计算并保留在“展示全部”中。
- 它们不进入 `eligibleIndexes`，因此不参与主要推荐。
- 最优视图不为它们生成“不生效”注释。

## 概率注释

最优视图：

- `unstable`：淡蓝词条名称，文案“X% 概率多 N枪，故不主要推荐，详情见全部展开”。
- `rare`：淡红词条名称，文案“X% 概率少 N枪，强烈推荐，详情见全部展开”。
- 仅词条名称着色，概率和说明保持普通颜色。
- 相同概率与差值的词条合并；每行最多 3 个词条。
- 刚好 3 个词条时，第一行词条末尾保留冒号，概率说明下一行显示。

展示全部：

- 对所有 `eligibleIndexes` 中存在多个枪数结果的词条显示详情。
- 注释以该词条表格中显示的严格最低枪数为基准，列出其他实际出现的枪数及概率。
- 不得拿全局推荐目标枪数与该词条自身结果比较，否则会给固定结果词条生成 `100% 概率多 N枪`。

## 历史回归清单

修改推荐或注释逻辑后至少验证：

1. 同一词条同时属于 `recommended` 和 `rare` 时只显示一次。
2. `unstable` 不出现在最优词条列表。
3. 相同概率注释中的词条名称不重复。
4. 高概率达到更低枪数时，目标枪数应直接降低，不应显示“高概率少一枪”。
5. 展示全部中，真正有枪数波动的有效词条必须有注释。
6. 单伤含无词条，双伤不含无词条。
7. 无头部命中时，头部专属正向词条不参与主要推荐，但仍出现在展示全部。
